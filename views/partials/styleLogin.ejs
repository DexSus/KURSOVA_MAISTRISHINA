<style>


/* Основные стили для тела страницы */
body {
  font-family: 'Arial', sans-serif;
  color: #333;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
  overflow: hidden; /* Это поможет избежать полос прокрутки, если фон выходит за границы */
  background: linear-gradient(135deg, #324B81, #47633F);
  background-size: 400% 400%;
  animation: gradientMove 15s ease infinite;
}

/* Анимация для плавного движения фона */
@keyframes gradientMove {
  0% {
    background-position: 0% 0%;
  }
  50% {
    background-position: 100% 100%;
  }
  100% {
    background-position: 0% 0%;
  }
}

/* Стиль контейнера формы */
.container {
  background: #ffffff;
  border-radius: 20px; /* Увеличиваем радиус скругления */
  box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
  padding: 40px; /* Увеличиваем отступы внутри контейнера */
  max-width: 500px; /* Увеличиваем максимальную ширину контейнера */
  width: 100%;
  border: 1px solid #ddd;
  transition: box-shadow 0.3s ease, transform 0.3s ease;
}

/* Эффект при наведении на контейнер */
.container:hover {
  box-shadow: 0 16px 32px rgba(0, 0, 0, 0.3);
  transform: scale(1.02);
}

/* Стиль для иконки */
.login-icon {
  display: block;
  margin: 0 auto 20px;
  max-width: 140px; /* Увеличиваем размер иконки */
  height: auto;
}

.register-icon {
  display: block;
  margin: 0 auto 20px;
  max-width: 140px; /* Увеличиваем размер иконки */
  height: auto;
}

/* Стиль для меток */
label {
  display: block;
  font-weight: 600;
  margin-bottom: 12px; /* Увеличиваем отступы для лучшего выравнивания */
  color: #47633F;
  font-size: 16px; /* Увеличиваем размер шрифта */
}

/* Стиль для текстовых полей и пароля */
input[type="text"],
input[type="password"],
select {
  width: 100%;
  padding: 16px; /* Увеличиваем внутренние отступы */
  border: 1px solid #ddd;
  border-radius: 10px; /* Увеличиваем радиус скругления */
  box-sizing: border-box;
  font-size: 16px;
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
  margin-bottom: 24px; /* Добавляем отступ снизу для равномерного пространства */
}

/* Стили для полей ввода при фокусе */
input[type="text"]:focus,
input[type="password"]:focus,
select {
  border-color: #324B81;
  box-shadow: 0 0 8px rgba(50, 75, 129, 0.2);
  outline: none;
}

/* Стиль для кнопки */
button {
  width: 100%;
  padding: 16px; /* Увеличиваем внутренние отступы */
  border: none;
  border-radius: 10px; /* Увеличиваем радиус скругления */
  background: linear-gradient(135deg, #324B81, #47633F);
  color: white;
  font-size: 18px; /* Увеличиваем размер шрифта */
  font-weight: 600;
  cursor: pointer;
  transition: background 0.3s ease, transform 0.2s ease;
  margin-top: 15px; /* Добавляем отступ сверху для отделения от полей ввода */
}

/* Эффект при наведении на кнопку */
button:hover {
  background: linear-gradient(135deg, #47633F, #324B81);
  transform: scale(1.02);
}

/* Стиль для ссылок */
a {
  display: block;
  text-align: center;
  margin-top: 20px; /* Увеличиваем отступ сверху */
  color: #324B81;
  text-decoration: none;
  font-size: 16px; /* Увеличиваем размер шрифта */
  transition: color 0.3s ease;
}

/* Эффект при наведении на ссылку */
a:hover {
  color: #47633F;
  text-decoration: underline;
}

/* Стили для заголовка */
h1 {
  font-size: 35px; /* Увеличиваем размер шрифта */
  font-weight: bold; /* Сделаем текст жирным */
  color: #324B81; /* Цвет текста */
  text-align: center; /* Центрируем текст */
  margin-bottom: 20px; /* Отступ снизу */
  position: relative; /* Для позиционирования псевдоэлементов */
}

/* Добавляем стиль подчеркивания под заголовок */
h1::after {
  content: '';
  display: block;
  width: 60%; /* Ширина линии */
  height: 4px; /* Высота линии */
  background: linear-gradient(135deg, #47633F, #324B81); /* Градиентная линия */
  border-radius: 2px; /* Скругление углов линии */
  position: absolute; /* Позиционируем линию */
  left: 50%; /* Выравнивание по центру */
  bottom: -10px; /* Отступ снизу от заголовка */
  transform: translateX(-50%); /* Центрирование линии */
}












</style>